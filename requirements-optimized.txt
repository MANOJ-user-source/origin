# Core Flask dependencies
Flask==2.3.2
Flask-SQLAlchemy==3.0.3
Flask-JWT-Extended==4.4.4
Flask-Migrate==4.0.4
Flask-CORS==4.0.0

# Database
psycopg2-binary==2.9.7

# Security
python-dotenv==1.0.0
PyJWT==2.8.0

# File handling
python-multipart==0.0.6

# HTTP client
requests==2.31.0

# Gunicorn for production
gunicorn==21.2.0

# Optional: Smaller ML alternatives (commented out for now)
# sentence-transformers==2.2.2  # Heavy - use only if needed
# torch==2.0.1  # Very heavy - consider alternatives
# transformers==4.30.2  # Heavy - use only if needed

# Development dependencies (remove in production)
Werkzeug==2.3.6
